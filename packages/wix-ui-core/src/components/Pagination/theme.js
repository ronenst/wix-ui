import {palette} from '../../palette';

export const core = {
  fontSize: '16px',
  borderRadius: '0',

  hover: {
    color: palette.black,
    backgroundColor: palette.grey,
    borderColor: palette.black
  }
};
